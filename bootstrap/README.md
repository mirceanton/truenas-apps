# DocoCD Bootstrap

1. Create the `/root/.doco-cd` directory in TrueNAS via SSH
2. Create the `/root/.doco-cd/1pw_token` file with a 1password service account token
3. Deploy this compose file to TrueNAS
